[package]
name = "project-kit"
version = "0.1.0"
edition = "2024"
description = "Project Bootstrap Protocol CLI — scaffold projects with RULES.md governance"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
